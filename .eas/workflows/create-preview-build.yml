name: Create Preview Build

on:
  push:
    branches:
      - develop

jobs:
  android_preview_build:
    name: Build Android
    type: build
    params:
      platform: android
      profile: preview

  ios_preview_build:
    name: Build iOS device
    type: build
    params:
      platform: ios
      profile: preview
